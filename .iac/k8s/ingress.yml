apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cezzis-com-cocktails-mcp-ingress
  namespace: cezzis
  labels:
    app.kubernetes.io/part-of: cezzis
  annotations:
    # k3s uses Traefik by default
    kubernetes.io/ingress.class: traefik
spec:
  rules:
    - host: cezzis-cocktails-mcp.127.0.0.1.sslip.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: cezzis-com-cocktails-mcp
                port:
                  number: 7999
