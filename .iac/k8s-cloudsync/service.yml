apiVersion: v1
kind: Service
metadata:
  name: cezzis-com-cocktails-mcp-cloudsync
  namespace: cezzis
  labels:
    app: cezzis-com-cocktails-mcp-cloudsync
    app.kubernetes.io/part-of: cezzis
    app.kubernetes.io/managed-by: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "300"
spec:
  type: ClusterIP
  selector:
    app: cezzis-com-cocktails-mcp-cloudsync
  ports:
    - name: http
      port: 7999
      targetPort: 7999
      protocol: TCP
