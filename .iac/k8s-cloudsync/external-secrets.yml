apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: cezzis-com-cocktails-mcp-cloudsync-secrets
  namespace: cezzis
  labels:
    app.kubernetes.io/part-of: cezzis
    app.kubernetes.io/managed-by: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "100"
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: cezzis-prm-secret-store
    kind: SecretStore
  target:
    name: cezzis-com-cocktails-mcp-cloudsync-secrets
    creationPolicy: Owner
    template:
      engineVersion: v2
      data:
        COCKTAILS_API_XKEY: "{{ .mcpCocktailsApiXKey }}"
        ACCOUNTS_API_XKEY: "{{ .mcpAccountsApiXKey }}"
  data:
    - secretKey: mcpCocktailsApiXKey
      remoteRef:
        key: cocktails-api-mcp-subscription-primary-key
    - secretKey: mcpAccountsApiXKey
      remoteRef:
        key: accounts-api-cezzis-com-subscription-primary-key


---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: cezzis-com-cocktails-mcp-cloudsync-shared-secrets
  namespace: cezzis
  labels:
    app.kubernetes.io/part-of: cezzis
    app.kubernetes.io/managed-by: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "100"
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: shared-prm-secret-store
    kind: SecretStore
  target:
    name: cezzis-com-cocktails-mcp-cloudsync-shared-secrets
    creationPolicy: Owner
    template:
      engineVersion: v2
      data:
        OTLP_HEADERS: "Authorization=Bearer {{ .otelApiKey }}"
        POSTGRES_PASSWORD: "{{ .postgresAppPassword }}"
  data:
    - secretKey: otelApiKey
      remoteRef:
        key: otel-collector-api-key
    - secretKey: postgresAppPassword
      remoteRef:
        key: postgres-app-password
